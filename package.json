{
  "name": "loeyae-aidlc",
  "version": "2.0.0",
  "description": "Loeyae AI-DLC v2 — engine-driven workflow with multi-harness distribution",
  "author": "Loeyae Team",
  "license": "MIT",
  "type": "module",
  "bin": {
    "loeyae-aidlc": "./bin/cli.js"
  },
  "files": [
    "bin/",
    "core/",
    "harness/",
    "scripts/",
    "dist/",
    "README.md"
  ],
  "scripts": {
    "build": "npx tsx scripts/build.ts",
    "build:kiro-crew": "npx tsx scripts/build.ts --harness kiro-crew",
    "build:kiro-ide": "npx tsx scripts/build.ts --harness kiro-ide",
    "build:kiro-cli": "npx tsx scripts/build.ts --harness kiro-cli",
    "build:claude": "npx tsx scripts/build.ts --harness claude",
    "build:opencode": "npx tsx scripts/build.ts --harness opencode",
    "build:all": "npx tsx scripts/build.ts --all",
    "prepare": "npm run build:all",
    "install-skill": "npx tsx bin/cli.ts install",
    "test": "python3 tests/test_engine_gates.py && python3 tests/test_evidence_producer.py && python3 tests/test_semantic_checkers.py && python3 tests/test_provider_config.py && python3 tests/test_mcp_config.py && python3 tests/test_provider_adapter.py && python3 tests/test_claude_plugin.py && python3 tests/test_platform_hooks.py && python3 tests/test_platform_layouts.py",
    "evidence": "npx tsx core/tools/aidlc-evidence.ts",
    "compile-graph": "npx tsx core/tools/aidlc-graph.ts compile",
    "scope-table": "npx tsx core/tools/aidlc-utility.ts scope-table"
  },
  "dependencies": {
    "tsx": "^4.0.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.0",
    "@types/node": "22.10.2",
    "typescript": "^5.5.0"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/loeyae/loeyae-aidlc-v2.git"
  }
}
