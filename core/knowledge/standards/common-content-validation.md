# 内容验证规则

## 强制要求：文件创建前进行内容验证

**关键**：所有生成的内容在写入文件之前必须经过验证，以防止解析错误。

## ASCII 图标准

**关键**：在创建任何包含 ASCII 图的文件之前：

1. **加载** `common-ascii-diagram-standards.md`
2. **验证**每个图表：
   - 统计每行字符数（所有行必须宽度一致）
   - 仅使用：`+` `-` `|` `^` `v` `<` `>` 和空格
   - 禁止使用 Unicode 制表符
   - 仅使用空格（禁止使用 Tab）
3. **测试**对齐方式，验证方框角是否垂直对齐

**详见 `common-ascii-diagram-standards.md` 中的模式和验证清单。**

## Mermaid 图表验证

创建或修改包含 Mermaid 图表的文件时：

1. 加载 `common-mermaid-diagram-standards.md`，确定图类型、信息边界、兼容目标和降级方式；
2. 实际生成、修改或排查 Mermaid 时加载 `common-mermaid-syntax-rules.md`；
3. 写入前执行语法规则中的静态检查，并核对图表与正文语义一致；
4. 仅在固定版本 Mermaid 解析器实际成功后声明“语法解析通过”；仅静态检查时必须明确未执行真实解析；
5. 已知无效且无法修复时移除 Mermaid 块，保留正文、表格或清单，不得写入无效图表。

Mermaid 是关系可视化，不是事实来源。文档在图表不渲染时仍须能从正文或表格理解关键事实，但不要求逐行复制图表内容。

## 通用内容验证

### 创建前验证清单
- [ ] 验证嵌入的代码块（Mermaid、JSON、YAML）
- [ ] 检查特殊字符转义
- [ ] 验证 Markdown 语法正确性
- [ ] 测试内容解析兼容性
- [ ] 为复杂元素包含备用内容

### 错误预防规则
1. **始终在使用工具/命令写入文件前进行验证**：绝不写入未经验证的内容
2. **转义特殊字符**：特别是在图表和代码块中
3. **提供替代方案**：为可视化内容包含文本版本
4. **测试语法**：验证复杂的内容结构

## 验证失败处理

### 当验证失败时
1. **记录错误**：记录验证失败的内容
2. **使用备用内容**：切换到基于文本的替代方案
3. **继续工作流**：不要因内容验证失败而阻塞流程
4. **通知用户**：说明由于解析限制使用了简化内容
