---
slug: build-and-test-templates
number: "3.7.1"
name: 构建测试模板
phase: construction
execution: CONDITIONAL
lead_agent: aidlc-quality-agent
support_agents: []
mode: inline
scopes: [feature, enterprise, mvp, classic]
consumes: []
produces: []
sensors: []
---

# 构建和测试 — 输出文档模板

> **加载时机**：仅在 `construction-build-and-test.md` 步骤 2-6 生成文档时加载。

---

## build-instructions.md 模板

```markdown
# 构建指令

## 前置条件
- **构建工具**：[工具名称和版本]
- **依赖**：[列出所有必需依赖]
- **环境变量**：[列出必需环境变量]
- **系统要求**：[操作系统、内存、磁盘空间]

## 后端构建步骤

### 1. 安装依赖
\`\`\`bash
[安装依赖的命令]
\`\`\`

### 2. 配置环境
\`\`\`bash
[设置环境的命令]
\`\`\`

### 3. 构建所有单元
\`\`\`bash
[构建所有单元的命令]
\`\`\`

### 4. 验证构建成功
- **预期输出**：[描述成功构建输出]
- **构建产物**：[列出生成的产物和位置]
- **常见警告**：[注明可接受的警告]

## 前端构建步骤

### 1. 安装依赖
\`\`\`bash
pnpm install
\`\`\`

### 2. 代码检查
\`\`\`bash
pnpm lint:all
\`\`\`

### 3. 构建
\`\`\`bash
pnpm build:dev   # 开发环境
pnpm build:prod  # 生产环境
\`\`\`

## 故障排除

### 构建因依赖错误失败
- **原因**：[常见原因]
- **解决方案**：[逐步修复]

### 构建因编译错误失败
- **原因**：[常见原因]
- **解决方案**：[逐步修复]
```

---

## unit-test-instructions.md 模板

```markdown
# 单元测试执行

## 运行单元测试
\`\`\`bash
[运行所有单元测试的命令]
# 后端：mvn test
# 前端：pnpm test:unit
\`\`\`

## 审查测试结果
- **预期**：[X] 个测试通过，0 个失败
- **测试覆盖率**：[预期覆盖率百分比]
- **测试报告位置**：[路径]

## 修复失败的测试
1. 审查测试输出
2. 识别失败的测试用例
3. 修复代码问题
4. 重新运行直到全部通过
```

---

## integration-test-instructions.md 模板

```markdown
# 集成测试指令

## 测试场景
### 场景 1：[单元 A] → [单元 B] 集成
- **描述**：[测试内容]
- **设置**：[环境设置]
- **测试步骤**：[执行步骤]
- **预期结果**：[应该发生什么]
- **清理**：[清理步骤]

### 场景 2：前后端联调
- **描述**：验证前端 API 调用与后端接口一致
- **设置**：启动后端服务，配置代理
- **测试步骤**：按页面模块逐一验证
- **预期结果**：所有接口返回正确数据

## 设置集成测试环境
\`\`\`bash
[启动服务命令]
[配置端点命令]
\`\`\`

## 运行集成测试
\`\`\`bash
[运行集成测试命令]
\`\`\`

## 清理
\`\`\`bash
[清理命令]
\`\`\`
```

---

## performance-test-instructions.md 模板（如适用）

```markdown
# 性能测试指令

## 性能需求
- **响应时间**：< [X]ms（[Y]% 请求）
- **吞吐量**：[X] 请求/秒
- **并发用户**：[X] 个
- **错误率**：< [X]%

## 运行负载测试
\`\`\`bash
[负载测试命令]
\`\`\`

## 分析结果
- **响应时间**：[实际 vs 预期]
- **吞吐量**：[实际 vs 预期]
- **错误率**：[实际 vs 预期]
- **瓶颈**：[识别的瓶颈]
```

---

## build-and-test-summary.md 模板

```markdown
# 构建和测试摘要

## 构建状态
- **构建工具**：[工具名]
- **构建状态**：[成功/失败]
- **构建产物**：[列出产物]
- **构建时间**：[时长]

## 测试执行摘要

### 单元测试
- **总数**：[X] | **通过**：[X] | **失败**：[X]
- **覆盖率**：[X]% | **状态**：[通过/失败]

### 集成测试
- **场景数**：[X] | **通过**：[X] | **失败**：[X]
- **状态**：[通过/失败]

### 性能测试
- **响应时间**：[实际]（目标：[预期]）
- **吞吐量**：[实际]（目标：[预期]）
- **状态**：[通过/失败/不适用]

### 额外测试
- **契约测试**：[通过/失败/不适用]
- **安全测试**：[通过/失败/不适用]
- **端到端测试**：[通过/失败/不适用]

## 整体状态
- **构建**：[成功/失败]
- **所有测试**：[通过/失败]
- **准备就绪**：[是/否]
```
