---
slug: code-review
number: "3.6"
name: 代码审查
phase: construction
execution: ALWAYS
lead_agent: aidlc-quality-agent
support_agents: []
mode: inline
scopes: [feature, enterprise, mvp, classic, express, workshop, bugfix, refactor]
consumes: []
produces: [docs/aidlc/construction/code-review.md]
sensors: []
requires: [code-generation]
---

# 代码审查

## 概述

派发独立审查者检查代码，在问题级联之前捕获它们。审查者获得精心构造的上下文进行评估——永远不是你的会话历史。

**核心原则**：所有任务都检查 Spec 与 Standards；只让审查调用数量随风险增长。

---

## 输入要求

执行任一审查模式前必须具备：

- 审查范围与变更集；
- 适用的 Spec、契约、设计和 Standards；
- 测试、构建与运行证据；
- 本文件“审查模式选择”确定的审查模式；
- 影响域。

复审还必须提供原问题、修复差异和必要回归证据。缺少必要输入时不得给出审查结论：通过能力入口调用时返回 `NEEDS_CONTEXT`，平台无能力入口时直接说明缺失项并暂停。

审查时机与模式由编排方按本文件“审查模式选择”和“审查时机”确定；执行审查的一方不得自行改变已确定的时机与模式。

### 章节职责归属

| 章节 | 消费方 |
|------|--------|
| 审查模式选择、审查时机、审查循环、最终全局审查（触发条件） | 编排方 |
| 与 AI-DLC 工作流的集成、审查记录 | 编排方 |
| 输入要求 | 执行审查的一方 |
| 独立双轴审查流程、Standards Axis 审查结果、规格合规检查、规格合规审查结果 | 执行审查的一方 |
| 双轴审查模式说明 | 执行方使用其检查与报告要求；其中审查记录格式由编排方写入时使用 |
| 最终全局审查清单（含用例点对账的检查与统计） | 执行审查的一方 |
| 用例点对账的门禁放行、产物文件生成、Construction 完成判定 | 编排方 |
| 处理审查反馈、对账未通过处理 | 实现者执行修复与补测试，编排方决定复审、回退步骤与升级 |
| 红旗信号、最终规则 | 双方共同适用 |

`FINAL_GLOBAL` 模式下，执行方必须逐项执行“最终全局审查清单”并给出 UC-D 覆盖统计结论；是否放行、是否生成对账产物、是否判定 Construction 完成由编排方决定。

执行方只产出审查结论与证据，不写 `state.md` 或审计文件、不放行质量门禁、不判定单元或 Construction 完成。

## 审查模式选择

| 模式 | 调用标识 | 适用条件 | 执行方式 |
|------|----------|----------|----------|
| 集成双轴审查 | `INTEGRATED_DUAL_AXIS` | `common-complexity-assessment.md` 判定为简单快速通道，且为单单元、无快速通道排除项 | 同一审查上下文依次完成 Spec 与 Standards 检查，分别记录结论 |
| 独立双轴审查 | `INDEPENDENT_DUAL_AXIS` | 中等/复杂流程，或多单元、跨组件/服务、契约/共享配置/数据所有权/安全边界风险 | Spec 与 Standards 使用独立视角或独立子 Agent，分别报告 |
| 最终全局审查 | `FINAL_GLOBAL` | 满足本文件“最终全局审查”触发条件 | 按最终全局审查清单执行一次跨单元审查 |

调用标识是编排方向 `aidlc-code-review` 传入审查模式的唯一取值来源。

复杂度升级后立即切换为独立双轴审查；不得以节省 token 为由降低审查模式。两种模式都必须修复关键/重要问题并复审，但复审只加载修复范围、原问题及必要回归上下文。建议级问题不阻塞复审通过。

---

## 审查时机

### 必须审查

| 时机 | 触发条件 |
|------|----------|
| 每个单元完成后 | 按审查模式完成 Spec + Standards 双轴检查 |
| 所有单元完成后 | 多单元、复杂/高风险，或跨组件/服务、契约、共享配置、数据所有权、安全边界时执行最终全局审查 |
| 合并前 | 确保整体一致性 |

**阻断语义**：上表"必须审查"中的三项均为硬性阻断——未完成审查或审查未通过时，后续步骤（C8 构建测试、Operations、合并）不得启动。不得以快速通道、时间紧迫或"已目测确认"为由跳过。编排方在进入 C8 前必须验证审计文件中审查记录存在且结论为通过（详见 `construction-build-and-test.md`"审查证据阻断"）。

### 建议审查（可选但有价值）

| 时机 | 原因 |
|------|------|
| 卡住时 | 新鲜视角可能发现盲点 |
| 重构前 | 建立基线 |
| 修复复杂 bug 后 | 确认修复没有引入新问题 |

---

## 独立双轴审查流程

> 本节适用于独立双轴审查。集成模式使用相同检查维度和结果格式，但在一次审查上下文中依次完成两轴，不再派发第二个审查者。

### Standards Axis：代码质量审查

**目的**：验证代码是否符合项目规范和质量标准。

**审查维度**：

#### 关键问题（必须修复，阻塞继续）

| 类别 | 检查项 |
|------|--------|
| 逻辑错误 | 条件判断错误、循环边界错误、空指针风险 |
| 安全漏洞 | SQL 注入、XSS、未授权访问、敏感数据泄露 |
| 数据风险 | 数据丢失可能、并发问题、事务不完整 |
| 异常处理 | 未捕获异常、异常吞没、错误的异常类型 |

#### 重要问题（应该修复）

**Fowler 代码 Smells 基线**（11种）：

| Smell | 定义 | 修复建议 |
|-------|------|----------|
| **Mysterious Name** | 命名不揭示意图 | 重命名；若无诚实名称则设计有问题 |
| **Duplicated Code** | 相同逻辑在多处出现 | 提取公共方法/类 |
| **Feature Envy** | 方法更多访问其他对象数据而非自身 | 将方法移到它羡慕的数据所在类 |
| **Data Clumps** | 相同字段/参数总是成组出现 | 封装为独立类型 |
| **Primitive Obsession** | 基本类型代表领域概念 | 创建领域小类型 |
| **Repeated Switches** | 相同 switch/if-cascade 多处重复 | 用多态或共享 Map 替代 |
| **Shotgun Surgery** | 一个变更导致多处分散修改 | 将变更聚合到一个模块 |
| **Divergent Change** | 一个模块因多个无关原因被修改 | 拆分模块，单一变更原因 |
| **Speculative Generality** | 为规格未要求的未来需求添加抽象 | 删除，回归直到真实需求出现 |
| **Message Chains** | 长 `a.b().c().d()` 调用链 | 在第一个对象后隐藏导航方法 |
| **Middle Man** | 类仅做委托转发 | 删除中间层，直接调用目标 |

**项目规范覆盖规则**：
- 以上 Smells 为基线判断
- 项目编码规范（MCP Skills + CLAUDE.md）优先
- 项目规范明确允许的情况，不标记为 Smell

**其他重要问题**：

| 类别 | 检查项 |
|------|--------|
| 命名 | 违反团队命名规范（Loeyae Boot / Vue 3） |
| 结构 | 方法过长（> 50行）、类职责不单一、层次混乱 |
| 测试 | 测试覆盖不足、测试质量差、缺少边界测试 |
| 规范 | 违反团队编码规范 |

#### 共享契约基线检查（条件）

- [ ] 声明型物化不含空实现、伪默认值、异常占位或可执行业务逻辑
- [ ] 不存在与 Owner 声明同职责的重复接口、DTO、枚举、Schema 或消费者私有副本
- [ ] 新增声明仅位于批准的 Owner 目标路径，未扩展到非 Owner 或未批准范围

#### 建议（可选修复）

| 类别 | 检查项 |
|------|--------|
| 性能 | 可优化的查询、不必要的循环、缓存机会 |
| 抽象 | 更好的设计模式、更清晰的抽象层次 |
| 文档 | 复杂逻辑缺少注释、公共 API 缺少文档 |

**Standards Axis 审查结果格式**：

```markdown
## Standards Axis 审查结果

**状态**：✅ 通过 | ⚠️ 需要修复

### 优点
- [做得好的地方，具体引用代码]

### 问题

**[关键]** — 必须修复
1. 文件：`path/to/file.java` L42
   问题：[描述]
   建议：[修复方案]

**[重要 — Smell: Feature Envy]**
1. 文件：`path/to/file.java` L78
   问题：[描述]
   建议：[修复方案]

**[重要 — 其他]**
1. 文件：`path/to/file.java` L90
   问题：[描述]
   建议：[修复方案]

**[建议]** — 可选
1. [描述 + 建议]

### 评估
- 关键问题：X 个
- 重要问题：X 个（Smells: Y 个, 其他: Z 个）
- 建议：X 个
```

---

### Spec Axis：规格合规审查

**审查清单**：

```markdown
## 规格合规检查

### 完整性（缺失检查）
- [ ] 需求文档中的每个功能点都已实现
- [ ] 用户故事中的每个验收标准都已满足
- [ ] 接口契约中定义的每个端点/方法都已实现
- [ ] 错误处理覆盖了规格中定义的所有异常场景
- [ ] **测试用例点覆盖**：本单元涉及的每个 UC-D-xxx（来自 `inception/application-design/test-cases/`）都有对应的 @TestCaseId 测试
- [ ] **用例点状态**：所有涉及的 UC-D 的 `status` 非 `blocked`（blocked 的须已在 I13 列入待产品决策清单）

### 准确性（偏差检查）
- [ ] 实现的行为与规格描述一致
- [ ] 数据模型与功能设计一致
- [ ] API 响应格式与契约一致
- [ ] 业务规则的实现与需求描述一致

### 无多余（过度实现检查）
- [ ] 没有添加规格未要求的功能
- [ ] 没有添加规格未要求的参数/字段
- [ ] 没有添加规格未要求的端点
- [ ] 没有"顺便"做的改进

### 接口契约合规
- [ ] 模块间调用符合 contracts.md 定义
- [ ] 请求/响应格式与契约一致
- [ ] 错误码与契约定义一致

### 共享契约基线合规（条件）
- [ ] 每项 `contract` 依赖关联的基线状态为 `verified`，并有实际验证证据
- [ ] 契约 ID、类型、边界、Owner、消费者、权威来源与目标代码路径和 I12/I14 产物一致
- [ ] 进程内声明符合 `shared-interfaces.md` 的批准设计；跨进程字段仍以机器契约为权威
- [ ] 消费者仅引用 Owner 的已验证声明，未重定义同职责契约

### UI 设计合规（条件：该单元涉及前端页面且 state.md `UI 设计方式` 非"跳过"）

比对基准按 `UI 设计方式` 确定：`html-mock` 对照 `docs/aidlc/inception/ui-mock/` 中的 mock-box；`figma` 对照页面对照表中 nodeId 对应的 Frame（通过 `get_design_context` / `get_screenshot` 获取）。

- [ ] 页面结构与设计基准的布局一致
- [ ] 表单字段（名称、类型、必填标记）与设计基准定义一致
- [ ] 操作按钮和交互行为覆盖设计基准中定义的所有用户操作
- [ ] 列表/表格的列定义与设计基准展示一致
- [ ] 业务状态展示（标签、颜色、图标）与设计基准一致
- [ ] 条件渲染逻辑（角色权限、状态驱动的显隐）与设计基准标注一致（html-mock 为业务说明；figma 为 Frame Annotation）
- [ ] 响应式/多端适配与设计基准标注的断点行为一致（如有标注）

**figma 模式附加检查**：
- [ ] 颜色、间距、字体使用项目 token，未直接硬编码 `get_design_context` 返回的 Tailwind 字面值
- [ ] 尺寸行为映射符合 `common-figma-design-standards.md` §2.3（`flex-1` 已补 `min-width: 0`）

**html-mock 模式附加检查**（条件：`UI 设计方式` = `html-mock`）：
- [ ] 组件样式中的色值、圆角、字号、间距、字体值与 Mock HTML 的 `<style>` 区块或其引用的 CSS 文件中的定义一致
- [ ] 项目存在全局设计 token 文件时，样式引用了对应的 CSS 变量（`var(--*)`），而非硬编码 Mock 中的字面值
- [ ] 未使用 UI 框架默认色值（Element Plus / Material / Ant Design 等内置色系）替代 Mock 定义的品牌色
- [ ] Mock 中定义的通用 class（如按钮/卡片/标签/布局容器）的属性值已在组件中逐项对齐

### 数据流完整性（强制）
- [ ] 每个数据读取操作都有对应的真实写入来源（非 Mock/硬编码）
- [ ] 依赖其他模块/单元的数据，该模块/单元已实现或在本次范围内
- [ ] 外部接口数据源已确认可用（非假设存在）
- [ ] 初始/种子数据的准备方案已落实（SQL 脚本、数据迁移、或录入入口）
- [ ] 列表/查询页面对应的数据写入入口已实现（不存在"能查不能增"的空页面）
- [ ] 数据源验证产物（`data-source-validation.md`）中无 🚨 无来源项
- [ ] **字段契约对齐**（关键/blocker）：代码中每个外部数据源字段引用与 C1 字段映射表（`data-source-validation.md` 字段映射表章节）或实际源码定义一致；发现不一致为阻断项，不得仅标注 warning 放行

**字段契约对齐审查方式**：
1. 提取代码中所有外部数据源属性访问（接口响应字段解构、DTO 属性引用、数据库列映射等）
2. 逐一与 C1 字段映射表中的"数据源字段路径"列或实际源码/契约定义交叉核对
3. 发现以下任一情况标记为 blocker：
   - 代码使用了字段映射表中不存在的字段路径
   - 代码中的字段名与映射表/源码定义拼写不一致
   - 代码使用了映射表中标记为🚨不存在且未标注解决方案的字段
4. C1 未产出字段映射表时（如快速通道无功能设计），以实际源码/契约定义为核对基准
```

**审查结果格式**：

```markdown
## 规格合规审查结果

**状态**：✅ 合规 | ❌ 不合规

### 如果不合规：

**缺失**（规格要求但未实现）：
1. [具体缺失项 + 规格引用]

**多余**（规格未要求但添加了）：
1. [具体多余项]

**偏差**（实现与规格意图不符）：
1. [具体偏差 + 期望行为 vs 实际行为]

**UI 设计不一致**（条件：涉及前端页面且 state.md `UI 设计方式` 非"跳过"）：
1. [具体不一致项 + 设计基准引用（HTML 文件+mock-box 或 Frame 名称+nodeId）+ 期望 vs 实际]

**数据流缺陷**（数据空中楼阁）：
1. [具体数据读取操作 + 缺失的写入来源 + 影响范围]

**字段契约不一致**（blocker，必须修复）：
1. [代码文件:行号 + 使用的字段路径 + 映射表/源码中的正确路径或"不存在"]

### 修复要求：
- [具体修复指令]
```

---

## 双轴审查模式说明

两个审查轴（Standards + Spec）**独立执行、独立报告**：

- 默认执行顺序为 Spec → Standards；两轴判断维度和结论互相独立，不因另一轴结果改变本轴结论。

```
┌──────────────────────┐
│ Standards Axis       │ → Standards 报告
├──────────────────────┤
│ Spec Axis            │ → Spec 报告
└──────────────────────┘
         ↓
    合并报告（不重排序，两轴各自保留）
         ↓
    是否有关键问题？
    是 → 修复循环
    否 → 审查通过
```

**为什么两轴独立报告？**
- Standards 通过但 Spec 失败 → 需求未满足
- Spec 通过但 Standards 失败 → 代码质量差
- 合并排序可能掩盖某轴问题

**审查记录格式**：

集成模式仍分别记录两轴结论，不得合并为单一“通过”：

```markdown
## 代码审查 — 单元 X
**审查模式**: 集成双轴审查
**Spec 结果**: ✅ 合规 | ❌ 不合规
**Standards 结果**: ✅ 通过 | ⚠️ 需要修复
**证据**: [各轴的规格引用与代码引用]
**修复状态**: [无需修复 | 已修复并复审通过 | 待修复]
```

独立模式使用以下完整记录：

```markdown
## 代码审查 — 单元 X
**时间戳**: [ISO 时间戳]
**审查模式**: 双轴独立审查
**Standards 结果**: ✅ 通过 | ⚠️ 需要修复
**Spec 结果**: ✅ 合规 | ❌ 不合规
**Standards 问题**: 关键 X 个, 重要 Y 个（Smells Z 个）, 建议 W 个
**Spec 问题**: 缺失 X 个, 偏差 Y 个, 多余 Z 个
**修复状态**: [已修复并重新审查通过 | 待修复]
```

---

## 审查循环

```
审查发现问题
  ↓
实现者修复
  ↓
重新审查（同一审查者视角）
  ↓
通过？→ 继续
未通过？→ 回到修复
```

**规则**：
- 审查发现关键或重要问题 = 必须修复 = 必须重新审查（建议级不阻塞）
- 不允许"修复了应该没问题了"跳过重新审查
- 重新审查只检查修复部分 + 修复是否引入新问题

---

## 最终全局审查

仅在多单元、复杂/高风险，或跨组件/服务、契约、共享配置、数据所有权、安全边界任务中执行。简单快速通道的单单元任务由单元双轴检查和 C8 影响域验证覆盖，并在审计中记录 C7 跳过原因。

触发后执行一次全局审查：

```markdown
## 最终全局审查清单

### 跨单元一致性
- [ ] 单元间接口调用正确
- [ ] 共享数据模型一致
- [ ] 错误处理策略统一
- [ ] 命名风格统一

### 集成完整性
- [ ] 所有单元能正确协作
- [ ] 依赖关系正确
- [ ] 配置完整（无遗漏的配置项）

### 非功能需求
- [ ] 性能要求满足（如有）
- [ ] 安全要求满足
- [ ] 日志和监控就位

### 测试完整性
- [ ] 单元测试覆盖率达标
- [ ] 集成测试覆盖关键路径
- [ ] 边界情况已测试

### 用例点对账（强制门禁，未通过 = Construction 未完成）

#### 对账① 设计覆盖（每个单元功能设计阶段执行，此处全局复核）
- [ ] I13 产出的每个 UC-D-xxx，都能在功能设计文档中找到对应的实现设计
- [ ] 无 UC-D 处于 `blocked` 状态未裁决（blocked 须有产品决策记录）

#### 对账② 测试映射（末尾强制）
- [ ] 每个 UC-D-xxx 都有至少一个带用例点标记的测试
- [ ] 所有标记测试全部通过（0 失败）
- [ ] 用例点覆盖统计：`已覆盖 UC-D 数 / 总 UC-D 数 = X/Y`，X=Y 才通过

**对账未通过处理**：
- 缺测试：回 TDD 补 RED 测试（带 @TestCaseId）
- 测试失败：修复实现，不是改测试期望
- 用例点 blocked：回 I13 待产品决策清单，不得绕过

**对账产物**：生成 `docs/aidlc/construction/audit/test-case-reconciliation.md`，列出每个 UC-D 的覆盖状态（已覆盖/缺测试/测试失败/blocked），作为 Construction 完成证据。

**机器校验辅助**（推荐实施，降低纯纪律门禁风险）：

项目中可添加如下最小校验脚本，在对账②时执行：

```bash
#!/bin/bash
# test-case-reconciliation-check.sh
# 用途：对比 _index.md 中的 UC-D 清单与测试文件中的标记，输出差集

# 1. 从 _index.md 提取所有 UC-D 编号
INDEX_FILE="docs/aidlc/inception/application-design/test-cases/_index.md"
EXPECTED=$(grep -oP 'UC-D-\d+' "$INDEX_FILE" | sort -u)

# 2. 从测试文件中提取所有已标记的 UC-D 编号
# 后端（Java/Kotlin）
BACKEND=$(grep -rhoP 'UC-D-\d+' src/test/ | sort -u)
# 前端（TypeScript/JavaScript）
FRONTEND=$(grep -rhoP 'UC-D-\d+' tests/ src/**/*.spec.* | sort -u)

COVERED=$(echo -e "${BACKEND}\n${FRONTEND}" | sort -u)

# 3. 输出差集
echo "=== 未覆盖的 UC-D ==="
comm -23 <(echo "$EXPECTED") <(echo "$COVERED")

echo "=== 覆盖统计 ==="
TOTAL=$(echo "$EXPECTED" | wc -l)
HIT=$(comm -12 <(echo "$EXPECTED") <(echo "$COVERED") | wc -l)
echo "已覆盖: $HIT / $TOTAL"
[ "$HIT" -eq "$TOTAL" ] && echo "✅ 对账通过" || echo "🔴 对账未通过"
```

> 此脚本为参考实现，项目可根据技术栈调整路径和正则。后续可升级为 MCP skill 提供更精确的 AST 级校验。

**对账纪律**：
- ❌ 不得为通过对账而删除/跳过 UC-D
- ❌ 不得为通过对账而改写 UC-D 的期望结果（改期望 = 改产品意图，走 CR）
- ❌ 不得用"代码逆向回规格"代替正向映射对账（逆向不可靠）

### 部署就绪
- [ ] 无硬编码配置
- [ ] 数据库迁移脚本就绪（如需）
- [ ] 文档更新（API 文档、README）
```

---

## 处理审查反馈

### 必须修复（关键 + 重要）

```
1. 修复问题
2. 运行测试确认无回归
3. 重新提交审查
```

### 不同意审查意见

如果认为审查意见不正确：

```
1. 提供技术理由说明为什么当前实现是正确的
2. 引用代码/测试证明
3. 如果审查者仍然坚持，升级给用户决定
```

**绝不**：
- 忽略关键问题
- 带着未修复的重要问题继续
- 不提供理由就拒绝反馈

---

## 与 AI-DLC 工作流的集成

### 在 Construction 阶段的位置

```
代码生成 — TDD 执行阶段
  ↓ 每个单元完成
按复杂度执行集成或独立双轴审查（本文件）
  ↓ 审查通过
标记单元完成，进入下一个单元
  ↓ 触发 C7？
是 → 最终全局审查；否 → 记录跳过依据
  ↓
构建和测试
```

### 审查记录

审查结果记录到单元审计文件 `docs/aidlc/construction/audit/{unit-id}.md`（每单元一份，包含 Spec Axis / Standards Axis / 修复与复审 / 验证证据索引四个章节）：

```markdown
## 代码审查 — 单元 X
**时间戳**: [ISO 时间戳]
**审查模式**: 双轴独立审查
**Standards 结果**: ⚠️ 需要修复
**Spec 结果**: ✅ 合规
**Standards 问题**: 关键 0 个, 重要 2 个（Smells 1 个）, 建议 1 个
**Spec 问题**: 缺失 0 个, 偏差 0 个, 多余 0 个
**修复状态**: 已修复并重新审查通过
```

---

## 红旗信号

**绝不：**
- 因为使用集成模式就遗漏 Spec 或 Standards 任一轴
- 将快速通道用于存在排除项或影响域不明的任务
- 忽略关键问题
- 带着未修复的重要问题继续
- 与有效的技术反馈争论（不提供证据）
- 在规格合规通过之前做质量审查
- 修复后不重新审查

---

## 最终规则

```
代码审查不是可选的；审查调用数量由风险决定。
两轴检查和问题闭环始终保留。
```
