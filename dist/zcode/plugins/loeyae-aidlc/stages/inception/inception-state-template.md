---
slug: state-template
number: "2.0"
name: 状态模板
phase: inception
execution: ALWAYS
lead_agent: aidlc-architect-agent
support_agents: []
mode: inline
scopes: [feature, enterprise, mvp, classic, express, workshop]
consumes: []
produces: []
sensors: []
completion_contract: instruction_only
requires: [workspace-detection]
---
# handoff.md 派生视图模板

> `docs/aidlc/aidlc-state.json` 是唯一机器状态。本模板只创建或更新 `docs/aidlc/handoff.md` 的人类协作视图；不得直接编辑机器 state，不得用 handoff 覆盖 stage、revision、审批或完成结果。恢复场景不得覆盖已有协作细节。

```markdown
# AI-DLC 协作交接视图

- **状态模式版本**：2

## 下一步交接
| 范围 | 更新时间 | 提示词 |
|------|----------|--------|
| {项目名} | {ISO时间} | `使用 AI-DLC，继续 {项目名}。请读取 docs/aidlc/handoff.md，从 {下一步骤} 恢复。` |

## 项目信息
- **项目类型**：{全新项目/存量项目}
- **协作模式**：{单人/团队协作}
- **架构模式**：{单模块/多模块}
- **审批模式**：{严格/标准/自主}
- **复杂度**：{简单/中等/复杂}
- **执行路径**：{快速通道/精简流程/完整流程}
- **开始时间**：{ISO时间}
- **当前阶段**：Inception
- **当前步骤**：I1 工作区检测
- **当前层级**：{产品级/模块级}
- **活跃模块**：{模块名/不适用}
- **活跃服务**：{服务 ID/不适用}
- **活跃单元**：{单元 ID/不适用}

## 场景与模块映射
| 场景 ID | 场景名 | 主模块 | 次要模块 | 装配清单路径 | 确认时间 |
|---------|--------|--------|----------|--------------|----------|

## 在途 PRD 参考资料
| 路径 | 版本 | 最后修改时间 | 编写方式 | 业务方回签 | 定位 | 登记时间 |
|------|------|--------------|----------|------------|------|----------|

## PRD 状态
| PRD 路径 | 版本 | 状态 | 更新时间 |
|----------|------|------|----------|

`状态` 仅可取 `draft / reviewed / approved`。未选择产出 PRD 时不创建文件，仅在阶段进度记录跳过依据。

## 活跃产品协调（条件）

需求、用户故事、UI 或其他设计存在未决/协调中的语义差异时创建；全部重审通过后移入阶段审计，并从 handoff.md 移除本区块。

| 协调 ID | 阶段/范围 | 差异摘要 | 状态 | 用户决策 | 重审范围 |
|---------|-----------|----------|------|----------|----------|
| {PC-001/UI-CF-001} | {I6/I8/I10/模块/页面} | {冲突来源与差异} | {pending_decision/reconciling/resolved} | {选项与被替换决定/待确认} | {受影响步骤} |

## 工作区状态
- **工作区根目录**：{绝对路径}
- **现有代码**：{是/否}
- **需要逆向工程**：{是/否}
- **后端语言/框架**：{检测结果}
- **Loeyae Boot 版本与 Starter**：{适用时填写/不适用}
- **前端技术栈/UI 框架**：{检测结果}
- **构建入口**：{实际命令或待确认}
- **测试入口**：{实际命令或待确认}
- **分布式系统**：{是/否/待确认}
- **技术适配**：{检测到的适配规则/不适用}
- **检测证据**：{依赖、配置或部署描述路径}

## UI 设计（I9 条件）

I9 路由判定后立即创建本区块，包括明确跳过的场景。

- **UI 设计方式**：{html-mock/figma/跳过}
- **Figma 来源**：{流程创建/外部提供/不适用}
- **产物位置**：{docs/aidlc/inception/ui-mock/ 或唯一主 Figma 文件链接/不适用}
- **页面计划**：{docs/aidlc/inception/ui-design/page-plan.md 或模块级路径/不适用}
- **页面计划状态**：{draft/approved/不适用}
- **设计状态**：{selected/file_created/designing/review_pending/blocked/reconcile_in_progress/approved/skipped}
- **当前批次**：{正整数/不适用}
- **下一操作**：{可直接恢复的动作/不适用}
- **涉及端**：{端名称列表/不适用}
- **页面数**：{数量/不适用}
- **设计风格**：{awesome-design 风格名/团队设计系统名/默认/不适用}
- **风格来源**：{awesome-design MCP/Figma 团队库/参考文件/默认/不适用}

### Figma 页面进度（Figma 模式条件）

| 模块 | 端 | 页面 | nodeId | 状态 | 最后验证 |
|------|----|------|--------|------|----------|
| {模块/不适用} | {端} | {页面名} | {nodeId/待创建} | {pending/in_progress/completed} | {get_screenshot 结果/未验证} |

`UI 设计方式` 是 Construction 阶段页面对照表、组件映射表和 `common-figma-design-standards.md` 加载条件的唯一判定依据，不得缺省。已有外部 Figma 设计稿必须记录为 `UI 设计方式：figma`、`Figma 来源：外部提供`，不得记录为“跳过”。

## 系统基线（条件）
- **基线路径**：{<system-baseline-root>/不适用}
- **代码版本标识**：{不可变提交 ID/标签或待确认}
- **制品标识/摘要**：{制品版本与摘要/尚未构建/不适用}
- **基线新鲜度**：{有效/待复核/过期/不适用}
- **服务与运行时依赖**：{路径/不适用}
- **契约索引**：{路径/不适用}
- **配置清单**：{路径/不适用}
- **一致性场景**：{路径/不适用}
- **本次受影响节点**：{服务、契约、配置、数据、外部系统/无}

## 共享契约基线（条件）

仅当 I14 的 `unit-of-work-dependency.md` 存在 `contract` 类型跨单元依赖时创建本区块；单单元项目或仅有 `implementation`、`runtime`、`none` 依赖时不得创建空表。

| 基线 ID | 范围 | 契约清单 | 状态 | 代码版本 | 验证证据 | 阻断原因 |
|---------|------|----------|------|----------|----------|----------|
| {SCB-范围标识} | {Owner 模块/范围} | {CT-xxx 列表} | {pending/in_progress/verified/blocked/change_requested} | {不可变代码版本} | {实际命令与结果/未验证} | {原因/-} |

- 状态含义、转换、验证与记录要求以 `construction-shared-contract-baseline.md` 为准；不得在此创建第二套状态机。
- `contract_ready` 仅由相关基线为 `verified` 派生，不在 handoff.md 单独持久化。

## 阶段进度
| 路由 | 步骤 | 状态 | 完成时间 | 产物/证据 |
|------|------|------|----------|-----------|
| I1 | 工作区检测 | in_progress | - | - |

## 单元与批次进度
| 模块 | 服务 | 批次 | 单元 | 状态 | 完成时间 | 验证证据 | 执行者 |
|------|------|------|------|------|----------|----------|--------|
| - | - | - | - | pending | - | - | - |

`rework_required` 是有效的单元状态：表示上游产品语义已变化，依赖旧语义的设计、代码或验证结论失效；该状态禁止调度。完成产品产物协调、更新单元定义并清除旧证据后转为 `pending`，再重新执行 Construction，验证通过后才能恢复完成状态。

## 技术用例执行映射（I13 条件）
| UC-D | 来源 | 执行范围 | 服务 | C8 证据 | 状态 |
|------|------|----------|------|---------|------|
| - | - | project/default | 不适用 | - | pending |

## 外部证据
| 证据 ID | 类型 | 运行标识 | 代码提交 | 制品标识/摘要 | 范围 | 结果 | 位置 | 时间 |
|---------|------|----------|----------|---------------|------|------|------|------|

## 质量门禁状态
| 阶段/步骤 | 时间 | 结果 | 证据/阻断原因 |
|-----------|------|------|---------------|

## 活跃变更请求
无

## 待优化项
无
```

## handoff.md 压缩规则（汇总替换）

`handoff.md` 只保留当前活跃状态；已完成的历史通过汇总行压缩。Git 历史和审计文件保留逐条明细。`场景与模块映射`、`在途 PRD 参考资料`、`PRD 状态` 是长期索引，**不参与压缩**。

### 自动压缩触发

| 时机 | 压缩范围 |
|------|----------|
| 批次内所有单元完成 | 该批次单元行压缩为一行汇总 |
| 阶段切换（如 Inception → Construction） | 上一阶段质量门禁压缩为汇总行 |
| CR 合并完成 | 活跃变更行移除（已由 PR 模型规定） |

### 手动压缩触发

用户说 `压缩 state` 时，执行全量压缩。

### 单元进度压缩格式

已完成批次中的所有单元行合并为一行汇总：

```markdown
| {模块} | {服务} | 批次1~N | 已完成 X/Y | summary | {最早}~{最晚完成时间} | 全部通过(git:{首commit}..{末commit}) | {执行者} |
```

仅保留 `pending`、`in_progress`、`rework_required`、`blocked` 状态的单元行逐条展示。

### 质量门禁压缩格式

已通过的历史阶段门禁合并为一行汇总：

```markdown
| {阶段名} 全部 | {最早}~{最晚时间} | 通过 | {N}/{N} 步骤通过 |
```

仅保留当前阶段和未通过/未执行的门禁逐条展示。

### 外部证据压缩格式

已关闭且不再活跃引用的证据行，按阶段合并：

```markdown
| EVD-{阶段}-SUMMARY | 汇总 | - | {首commit}..{末commit} | - | {阶段名} | {N}项全部通过 | state-archive | {时间范围} |
```

### 压缩安全规则

- 压缩前确认所有被压缩行状态为 `completed`/通过；不压缩 `blocked`、`pending` 或 `in_progress`。
- 汇总行必须保留 Git commit 范围，支持 `git log {首}..{末}` 追溯明细。
- 压缩操作本身作为一次 Git commit（消息：`chore: 压缩 handoff.md 已完成进度`）。
- 审计文件 `docs/aidlc/construction/audit/` 中的逐单元明细不受压缩影响。

## 填充规则

- 所有动态字段必须替换为检测结果；不适用字段写“不适用”，不得保留占位符。
- 条件步骤只有在路由判断后才追加到阶段进度，跳过时记录原因。
- 分布式能力不存在时，系统基线字段统一写“不适用”；存在时只记录路径、新鲜度和受影响节点，不复制完整目录内容。
- 外部证据必须包含不可变代码提交以及适用的制品标识/摘要，并与本次验证目标一致；缺失或无法比较时记录“未验证”。
- `单元与批次进度` 在 I14 完成后填充；I14 跳过时创建 `project/default` 行承接 I13 用例和 C8 证据。每个单元必须有服务归属或“不适用”。
- `UI 设计` 区块在 I9 路由判定后立即填充。`UI 设计方式` 只能取 `html-mock`、`figma`、`跳过` 三值之一；`Figma 来源` 仅在 figma 模式取 `流程创建` 或 `外部提供`。`页面计划` 必须指向跨模式唯一的 `page-plan.md`，`页面计划状态` 由 I9 编排层在用户确认前后维护为 `draft/approved`；跳过时两项均写“不适用”。Construction 阶段据此选择页面对照表格式和还原规范，取值错误会导致代码生成断裂。
- I3 完成后立即维护 `场景与模块映射` 和 `在途 PRD 参考资料`。选择产出 PRD 时增量维护 `PRD 状态`，状态流转为 `draft → reviewed → approved`。
- 任何步骤发现产品语义差异时创建或更新 `活跃产品协调`；未决时为 `pending_decision`，用户确认基准并开始同步产物后为 `reconciling`。未决协调为零且受影响审查全部通过后，移入阶段审计并从 handoff.md 活跃状态移除。
- I10 发现产品语义差异时，`设计状态` 必须为 `blocked`；协调开始后改为 `reconcile_in_progress`，全部适用审查通过后才能改为 `approved`。
- 上游语义变化后，将所有依赖旧语义且尚无代码基线的单元标记为 `rework_required`，并清除或标记失效的旧验证证据。受影响范围是否已有代码基线，以存量实现证据或对应 C8 通过证据判定，不新增脱离证据的全局布尔值。
- Figma 模式必须增量更新 `设计状态`、`当前批次`、`下一操作` 和 `Figma 页面进度`；创建文件或验证外部链接后立即记录唯一主文件 URL，不得等 I9 全部完成后一次性写入。
- handoff.md 是阶段、批次和单元恢复的唯一状态源。
